import React from "react";
import ReactDOM from "react-dom/client";
import AppRelaxify from "./AppRelaxify";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <AppRelaxify />
  </React.StrictMode>
);
